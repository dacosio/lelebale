import React from 'react';
import { PackageGeneratedProps } from './Package.props';
import { Container } from './Package.style';

const PackageView = (props: PackageGeneratedProps) => {
  return <Container> Package </Container>;
};

export default PackageView;
